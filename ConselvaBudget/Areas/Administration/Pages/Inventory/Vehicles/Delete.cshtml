@page
@inject IStringLocalizer<Resources.SharedResource> Localizer
@model DeleteModel

@{
    Layout = "/Pages/Shared/_DeleteLayout.cshtml";
    ViewData["Title"] = $"{Localizer["DELETE"]} {Localizer["ENTITY_VEHICLE_NAME"]}";
}

<dt class="col-sm-2">
    @Html.DisplayNameFor(model => model.Vehicle.Name)
</dt>
<dd class="col-sm-10">
    @Html.DisplayFor(model => model.Vehicle.Name)
</dd>

@section DeleteFormContents {
    <input type="hidden" asp-for="Vehicle.Id" />
    <delete-confirm-button></delete-confirm-button>
    <cancel-button asp-page="./Index"></cancel-button>
}
