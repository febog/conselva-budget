@page
@inject IStringLocalizer<Resources.SharedResource> Localizer
@inject IHtmlLocalizer<Resources.SharedResource> HtmlLocalizer
@model EditModel

@{
    Layout = "/Pages/Shared/_CreateEditLayout.cshtml";
    ViewData["Action"] = @Localizer["EDIT"];
    ViewData["Entity"] = @Localizer["ENTITY_VEHICLE_NAME"];
}

<div asp-validation-summary="ModelOnly" class="text-danger"></div>
<input type="hidden" asp-for="Vehicle.Id" />
<div class="col-12">
    <label asp-for="Vehicle.Name" class="form-label"></label>
    <input asp-for="Vehicle.Name" class="form-control" />
    <span asp-validation-for="Vehicle.Name" class="text-danger"></span>
</div>
<div class="col-12">
    <save-button></save-button>
    <cancel-button asp-page="./Index"></cancel-button>
</div>

@section PreForm {
    <h2 class="h4">@Localizer["ENTITY_VEHICLE_CATALOG_NAME"]</h2>
    @HtmlLocalizer["ENTITY_VEHICLE_HELP"]
}
