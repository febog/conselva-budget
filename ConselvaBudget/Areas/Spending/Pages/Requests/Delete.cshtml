@page
@model DeleteModel

@{
    Layout = "/Pages/Shared/_DeleteLayout.cshtml";
    ViewData["Title"] = "Delete Request";
}

<dt class="col-sm-2">
    @Html.DisplayNameFor(model => model.ExpensesRequest.Description)
</dt>
<dd class="col-sm-10">
    @Html.DisplayFor(model => model.ExpensesRequest.Description)
</dd>

@section DeleteFormContents {
    <input type="hidden" asp-for="ExpensesRequest.Id" />
    <input class="btn btn-danger" type="submit" value="Delete">
    <a class="btn btn-secondary" asp-page="./Index">Cancel</a>
}
